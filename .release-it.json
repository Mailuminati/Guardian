{
  "$schema": "https://unpkg.com/release-it/schema/release-it.json",
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true
  },
  "github": {
    "release": true,
    "releaseName": "v${version}"
  },
  "npm": false,
  "hooks": {
    "after:bump": "sed -i '' 's/EngineVersion.*= \".*\"/EngineVersion         = \"${version}\"/' mi_guardian/globals.go && sed -i '' 's/GUARDIAN_VERSION=\".*\"/GUARDIAN_VERSION=\"${version}\"/' install.sh && git add mi_guardian/globals.go install.sh"
  }
}
